const assert=require('node:assert/strict'),fs=require('node:fs'),http=require('node:http'),path=require('node:path');const {chromium}=require('playwright');
(async()=>{const root=process.cwd();const server=http.createServer((req,res)=>{const file=path.resolve(root,req.url==='/'?'index.html':req.url.slice(1));if(!file.startsWith(root+path.sep)||!fs.existsSync(file)){res.writeHead(404);return res.end();}res.setHeader('Content-Type',({'.html':'text/html','.js':'text/javascript','.css':'text/css'})[path.extname(file)]||'application/octet-stream');res.end(fs.readFileSync(file));});await new Promise(r=>server.listen(0,'127.0.0.1',r));const base=process.env.TEST_BASE_URL||'http://127.0.0.1:'+server.address().port;const browser=await chromium.launch({headless:true,executablePath:'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe'});
try{for(const width of [390,1366]){const context=await browser.newContext({viewport:{width,height:900},geolocation:{latitude:31.518,longitude:74.262},permissions:['geolocation']});await context.route('**/*',r=>r.request().url().startsWith(base)?r.continue():r.fulfill({contentType:'text/javascript',body:''}));const page=await context.newPage();await page.goto(base);await page.addStyleTag({content:'*{animation:none!important;transition:none!important;scroll-behavior:auto!important}'});await page.locator('#main-location-input').fill('Yateem Khana');await page.locator('#stop-search-suggestions [data-stop-key]').first().click();await page.locator('.brand-logo').click();await page.locator('#main-location-input').press('Escape');await page.locator('#btn-locate-me').click();await page.waitForFunction(()=>appState.currentLocation?.source==='gps');await page.waitForFunction(()=>!document.querySelector('#search-loading-overlay').classList.contains('active'));console.log(width+' after GPS',await page.evaluate(()=>({oldSelection:appState.selectedStopSuggestion?.stopName,primary:appState.recommendationResults.matchingRoutes[0].stop.name})));await page.locator('.brand-logo').click();await page.locator('#main-location-input').press('Escape');await page.locator('#btn-find-bus').click();console.log(width+' after Find',await page.evaluate(()=>({source:appState.currentLocation?.source,primary:appState.recommendationResults?.matchingRoutes[0]?.stop.name})));assert.equal(await page.evaluate(()=>appState.currentLocation?.source),'gps');assert.equal(await page.evaluate(()=>appState.recommendationResults.matchingRoutes[0].stop.name),'Flat Stop');await context.close();}}finally{await browser.close();await new Promise(r=>server.close(r));}})().catch(e=>{console.error(e);process.exitCode=1});
